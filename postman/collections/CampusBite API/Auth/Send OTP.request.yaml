$kind: http-request
method: POST
url: '{{baseUrl}}/api/auth/send-otp'
order: 1000
description: Send OTP to a Ghana phone number for authentication
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "phone": "{{phone}}"
    }
